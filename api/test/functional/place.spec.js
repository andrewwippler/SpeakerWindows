'use strict'

const { test, trait } = use('Test/Suite')('Place')
const Place = use('App/Models/Place')

trait('Test/ApiClient')

test.skip('Get list of places on specific illustration', async ({ client }) => {

})

test.skip('Can add new place to illustration', async ({ client }) => {

})
