'use strict'

const { test, trait } = use('Test/Suite')('Illustration')
const Illustration = use('App/Models/Illustration')

trait('Test/ApiClient')

test('Create Illustrations', async ({ client }) => {


})

test('Illustration is not present in unrelated tag', async ({ client }) => {


})
